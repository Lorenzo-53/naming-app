import 'package:flutter/material.dart';

class HomePagee extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Home Page")),
      body: Center(child: Text("hii", style: TextStyle(fontSize: 40))),
    );
  }
}
